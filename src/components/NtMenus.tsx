export const NtMenuItem = ({ children }: { children: React.ReactNode }) => {
	return <div>{children}</div>
}

export const NtMenus = ({ children }: { children: React.ReactNode }) => {
	return <div>{children}</div>
}
